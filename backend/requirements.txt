fastapi>=0.109.0
hypercorn>=0.14.4
























































































































































































































































    return _llm_service        _llm_service = LLMService()    if _llm_service is None:    global _llm_service    """Get or create LLM service instance"""def get_llm_service() -> LLMService:_llm_service: Optional[LLMService] = None# Global instance        }            "feedback": "Description covers main elements with some detail.",            },                "detail": 17,                "accuracy": 16,                "vocabulary": 16,                "grammar": 16,            "breakdown": {            "cefr_level": "B1",            "score": 65.0,        return {        """Return a sensible default result"""    def _get_default_result(self) -> dict:            return None            logger.error(f"[LLMService] Failed to parse JSON from response")        except json.JSONDecodeError:            return None                    return result                ):                    for key in ["score", "cefr_level", "breakdown", "feedback"]                    key in result                if all(                # Validate structure                result = json.loads(json_str)                json_str = response[start:end]            if start >= 0 and end > start:            end = response.rfind("}") + 1            start = response.find("{")            # Try to find JSON in the response        try:        """Extract JSON from model response"""    def _parse_response(self, response: str) -> Optional[dict]:[END]"""}}  "feedback": "<1-2 sentence feedback>"  }},    "detail": <number 0-25>    "accuracy": <number 0-25>,    "vocabulary": <number 0-25>,    "grammar": <number 0-25>,  "breakdown": {{  "cefr_level": "<A1|A2|B1|B2|C1>",  "score": <number 0-100>,{{Provide a JSON response with this exact structure (no extra text, only JSON):User's Description: {user_response}Expected Keywords/Concepts: {keywords_str}Reference Description: {reference_description}        return f"""You are an English language assessment expert. Evaluate the following picture description.        keywords_str = ", ".join(keywords)        """Build the evaluation prompt"""    ) -> str:        self, user_response: str, keywords: list[str], reference_description: str    def _build_prompt(            return None            logger.exception(e)            logger.error(f"[LLMService] ❌ Evaluation failed: {e}")        except Exception as e:            return result            logger.info("=" * 80 + "\n")            logger.info("[LLMService] Status: SUCCESS ✓")            logger.info("[LLMService] ========== EVALUATION COMPLETE ==========")            logger.info("\n" + "=" * 80)            logger.info(f"[LLMService]   Feedback: {result['feedback']}")            logger.info(f"[LLMService]     - Detail: {result['breakdown']['detail']}")            )                f"[LLMService]     - Accuracy: {result['breakdown']['accuracy']}"            logger.info(            )                f"[LLMService]     - Vocabulary: {result['breakdown']['vocabulary']}"            logger.info(            logger.info(f"[LLMService]     - Grammar: {result['breakdown']['grammar']}")            logger.info(f"[LLMService]   Breakdown:")            logger.info(f"[LLMService]   CEFR Level: {result['cefr_level']}")            logger.info(f"[LLMService]   Score: {result['score']}/100")            logger.info("\n[LLMService] STEP 4: EVALUATION RESULT")            # Log the result            logger.info(f"[LLMService]   ✓ Output parsed successfully")                result = self._get_default_result()                logger.warning("[LLMService] Failed to parse response, using defaults")            if result is None:            result = self._parse_response(raw_output)            logger.info(f"[LLMService]   Raw output length: {len(raw_output)} chars")            logger.info("\n[LLMService] STEP 3: PARSING MODEL OUTPUT")            # Parse the response            )                f"[LLMService]   Tokens used: {response['usage']['completion_tokens']}"            logger.info(            logger.info(f"[LLMService]   ✓ Inference complete")            raw_output = response["choices"][0]["text"].strip()            )                stop=["[END]"],                top_p=0.9,                temperature=0.3,                max_tokens=200,                prompt,            response = LLMService._llm(            logger.info("[LLMService]   Starting inference...")            logger.info("[LLMService]   Max tokens: 200")            logger.info("[LLMService]   Temperature: 0.3 (deterministic)")            logger.info("[LLMService]   Model: TinyLlama-1.1B-GGUF")            logger.info("\n[LLMService] STEP 2: RUNNING INFERENCE")            # Run inference            logger.info(f"[LLMService]   Keywords: {', '.join(keywords)}")            )                f"[LLMService]   User response length: {len(user_response)} chars"            logger.info(            logger.info(f"[LLMService]   Prompt length: {len(prompt)} characters")            prompt = self._build_prompt(user_response, keywords, reference_description)            logger.info("\n[LLMService] STEP 1: BUILDING EVALUATION PROMPT")            # Build the evaluation prompt            logger.info("=" * 80)            logger.info("[LLMService] ========== STARTING LLM EVALUATION ==========")            logger.info("=" * 80)        try:            return None            logger.error("[LLMService] Model not initialized!")        if not self.is_initialized():        """            Dict with score, level, breakdown, and feedback or None if failed        Returns:            reference_description: Reference/example description            keywords: Expected keywords/concepts to identify            user_response: User's description of the picture        Args:        Evaluate a picture description using LLM        """    ) -> Optional[dict]:        reference_description: str,        keywords: list[str],        user_response: str,        self,    def evaluate_description(        return LLMService._llm is not None        """Check if model is loaded and ready"""    def is_initialized(self) -> bool:            return False            logger.error(f"[LLMService] ❌ Failed to initialize model: {e}")        except Exception as e:            return False            )                "Install with: pip install llama-cpp-python"                "[LLMService] ❌ llama-cpp-python not installed. "            logger.error(        except ImportError:            return True            logger.info("[LLMService] ✓ Model loaded successfully")            )                verbose=False,                n_threads=8,  # Use 8 CPU threads                n_gpu_layers=-1,  # Use GPU if available                model_path=self.model_path,            LLMService._llm = Llama(            logger.info("[LLMService] Initializing Llama model...")            from llama_cpp import Llama            # Import here to avoid import errors if package not installed            logger.info(f"[LLMService] Loading GGUF model from {self.model_path}")                return False                logger.info("[LLMService] Model will be downloaded on first use")                )                    f"[LLMService] Model file not found at {self.model_path}"                logger.warning(            if not os.path.exists(self.model_path):            logger.info("[LLMService] Initializing LLM model...")        try:        """Load the GGUF model into memory"""    def initialize(self):        logger.info(f"[LLMService] Configured model path: {self.model_path}")        )            str(Path.home() / ".thingual" / "models" / "tinyllama.gguf"),            "GGUF_MODEL_PATH",        self.model_path = os.getenv(            return  # Already initialized        if LLMService._llm is not None:        """Initialize LLM service"""    def __init__(self):        return cls._instance            cls._instance = super(LLMService, cls).__new__(cls)        if cls._instance is None:    def __new__(cls):    _model_path = None    _llm = None    _instance = None    """Service for running LLM inference on picture descriptions"""class LLMService:logger = logging.getLogger(__name__)import jsonfrom pathlib import Pathfrom typing import Optionalimport osimport logging"""Uses llama-cpp-python to run GGUF models locallysqlalchemy>=2.0.25
psycopg2-binary>=2.9.9
bcrypt>=4.1.2
python-jose[cryptography]>=3.3.0
pydantic>=2.5.3
pydantic-settings>=2.1.0
python-dotenv>=1.0.0
google-auth-oauthlib>=1.2.0
google-auth-httplib2>=0.2.0
httpx>=0.26.0
alembic>=1.13.1
email-validator>=2.1.0
phonenumbers>=8.13.0
python-multipart>=0.0.6

# LLM inference for picture description assessment
llama-cpp-python>=0.2.84

